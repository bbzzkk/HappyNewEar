import styled from "@emotion/styled";

const FooterLayout = styled.div((props) => ({
    gridRowStart: '2',
    gridRowEnd: '3',
    textAlign: 'center',
    height: '10em'
}))

export default FooterLayout;